import { useOutletContext } from "react-router-dom";

function Notes()
{

    

    return (<>
    
        <div className="default" id="change">
            <p id="title" className="default">Select a note, or create a new one.</p>
        </div>
    
    
    </>);
}

export default Notes;